aioConfig {
    publishingAio {
        description = 'Slim Bus Android Core Library'
        labels = ['android','java','eventbus']
    }
}

apply plugin: 'android-lib-aio'
apply plugin: 'publishing-aio'

dependencies {
    compile project(':slimbus-core')
}
